# G7 gates: retrained models on ISCXVPN2016 and the table-size rule

Generated by `scripts/iscx_models.py`. Retrained NetBeacon-style models (scikit-learn trees; **not** the shipped ones), cross-fit by session-pair group; held-out flows only. Classes: Messaging, Streaming, File transfer, P2P, VoIP. Packet-level macro-F1 against the capture label.

## G7-0: does the full-state path beat the per-packet fallback?

| population | full model macro-F1 (isolated) | per-packet fallback macro-F1 | gap |
|---|---|---|---|
| flows with more than 50 packets (primary) | 0.347 | 0.654 | -0.307 |
| flows with more than 50 packets, P2P excluded | | | -0.225 |
| all flows | 0.351 | 0.650 | -0.299 |

Pre-registered rule: at least 0.10 proceeds, 0.03 to 0.10 is descriptive only, below 0.03 kills the workload. **Verdict: KILL.**

## Folds and models

| fold | gate threshold (packets) | training flows | phase-event rows (2, 4, 8, 32, 256, 512, 2048) | phase trees trained |
|---|---|---|---|---|
| 0 | 436 | 12,624 | 12573, 1020, 832, 542, 180, 163, 92 | [2, 4, 8, 32, 256, 512, 2048] |
| 1 | 356 | 4,814 | 4726, 1224, 1031, 450, 244, 164, 68 | [2, 4, 8, 32, 256, 512, 2048] |

## Table-size rule (from concurrency only)

Alive flows that took a slot in the isolated run, 10 s samples: median 195, p10 5, p90 239, max 311. Smallest power of two, at least 512, with median occupancy at most 7%: **N = 4,096 slots** (occupancy 4.8%).

Flows: 17,459 in total, 666 with more than 50 packets.

Deviation from the pre-registration, stated: the retrained trees are evaluated directly by the emulator; no switch-table quantisation of them is modelled (G7-1 is therefore the adapter contract and tree-agreement tests in `tests/test_dgrade_sklearn_models.py`, not a table-versus-tree comparison). The gate is out-of-fold for scored flows only: the other fold's flows are replayed with the same fold's models.
