# Check: the shipped flow-size model admits the holder pattern

Generated by `scripts/g6_gate_check.py`. Holder packets are 60-byte TCP packets (TTL 64, window 1024, data offset 5) from `build_fill`. The shipped flow-size model (a long flow is one with a score above 50) is applied to the holder packets.

- Holder flows examined: 655; packets: 46,317.
- Flow-size score of the first packet: mean 65.0; share above 50 (predicted long): 100%.
- Share of all holder packets scored above 50: 100%.
- Per-packet verdict codes on holder packets (code: packets): {1: 46317}.

The emulator forces holders long and gives them per-packet code 1 (`g2b_mawi.py`); for this header pattern the shipped model agrees on both. Holders stay below 2,000 packets, and their phase verdicts are evaluated with the shipped tables like any other flow. This checks one header pattern, not every packet an attacker could send.
