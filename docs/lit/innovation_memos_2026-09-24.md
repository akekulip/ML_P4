# Innovation memos, 2026-09-24 (research-direction brainstorm; nothing here has been run)

Requested by Philip after the fidelity-gate work started, to drive new ideas rather than only verify. Six specialist agents were launched; four returned (PI, sdn-networks-expert, research-scientist here; p4-dataplane-engineer pending), two hit the session rate limit (literature-reviewer, power-systems-expert) and are queued for retry after the reset at 2:10pm. Every idea below is a hypothesis with a proposed cheap experiment, not a result. Full transcripts are in the session; this file keeps the rankings and the top candidates.

## PI memo: eight reframings, ranked
Top 2 (novelty x testability x venue fit, both scored 100): **(1) self-downgrade evasion** — an attacker forces its own malicious flow onto the weak fallback (1 holder unkeyed, 2 under D4c), turning small mean accuracy harm into a targeted evasion result on PeerRush's botnet classes; cheapest test: rerun `g6_targeted.py` with victims = malicious-labelled flows, compare full/fallback/targeted recall. **(2) identity aliasing as an integrity attack** — the XOR-fold hash lets a crafted tuple alias a victim's slot AND tag, corrupting features rather than displacing them (a different, likely worse, attack class); cheapest test: add an `xor_fold` hash mode, enumerate alias classes, replay with merged alias packets, measure verdict flips. Also ranked: (3) spatial two-choice across two switches instead of a second hash, (6) per-prefix slot quotas with a real (non-constant-factor) guarantee, (3)+(4) a mean-field phase-diagram theory with a parametric workload generator, (5) a price-of-a-slot frontier (formalizes why D1/D3/rent failed), (7) classifier-aware admission by fallback margin, (8) a workload generator as a support tool. PI recommends starting with (1), auditing (2) in parallel.

## SDN-networks-expert memo: seven new mechanisms, ranked
**Top 2 to test first:** **A. lease plus verdict memo ("classify then release")** — freeze a phase-k verdict into the existing memo table so the slot becomes takeable once a flow is provisionally classified, attacking the high-load failure directly; predicted R about 0.9+ against a non-rotating attacker, 0.1-0.3 against a rotating one (so the rotating-holder arm is the real test); Tofino cost about 0 stages in a controller-table variant. **B. off-path spill with verdict return** — refused flows get mirrored to a software classifier whose verdict is written back to the memo; hash-independent, gives a ceiling for any slot-side scheme (predicted R 0.9+ at all loads with unlimited capacity); near-zero switch cost. Also ranked: C. partial-key cuckoo one-kick relocation (refusal falls toward rho^4; stage-1 is already full so low confidence it fits), D. per-ingress-port admission budget (immune to hash knowledge, needs a non-spoofable resource), G. contention-adaptive gate threshold (fixes benign-only contention, not the attack), H. pinned reservations from controller priors (good for OT's static session sets, near-zero coverage on MAWI), F. tiered mid-accuracy model on collision-tolerant sketch features. Rejected: random replacement (zero-sum), rekeying (already falsified by G6 addendum 4).

## Research-scientist memo: quantitative findings plus seven methodological ideas
**Two findings computed for free from existing tables (not new runs):** (A) the D4s "2 rho^2 vs rho^2" explanation is quantitatively close (bucket-of-2 formula gives a ratio of 1.75-1.9 against the observed 2.03); (B) the iso-refusal attacker-cost multiple of 3.7x overstates cost at iso-*harm*: comparing excess at equal absolute harm gives a multiple closer to 2.4-2.5x, about the same as doubling the table would cost. **Top idea (1): an instrumented mechanistic model** fit on already-run sweeps, with a concrete miss: the naive marginal law over-predicts recovery at high load by 0.03 (low load) growing to 0.2-0.6 (high load), always in the same direction — a real mechanism gap (candidate: holders get a higher admission rate under two-choice than benign newcomers do, or effective occupancy is undercounted). Also ranked: (2) a phase-diagram figure with theory contours, (3) new metrics (flow-time on the accurate path, downgrade half-life, accuracy-at-risk), (4) variance-reduction (control variates, common random numbers, an influence-function estimator) to fix the PeerRush marginal power problem, (5) a causal 2x2 with a placebo attacker and a d=3 dose-response arm, (6) a parametric benign-workload generator, (7) packet-concentration and a labelled-to-unlabelled "transfer coefficient" so MAWI's flow counts get an implied accuracy prediction. Drafted (not committed) pre-registration text for a "G9" instrumented-model entry.

## Open items
- p4-dataplane-engineer's hardware-native memo (new Tofino primitives) is still running; power-systems-expert (OT framing, second-workload candidates) and literature-reviewer (cross-field techniques: cache admission, Rowhammer defences, heavy-hitter sketches, cuckoo filters, SYN cookies) hit the rate limit and are queued for after 2:10pm.
- None of this is pre-registered or run. Before touching anything: finish the G8 fidelity gate in progress (the plan's current step), then bring these ideas to Philip for a decision on which one or two to pre-register next. The self-evident cheap, mostly-free checks (research-scientist's Findings A and B; the phase-k verdict cost table for idea A/1) can be computed from existing saved outputs without new emulation and are good first moves once G8 closes.

## P4-dataplane-engineer memo: hardware-native mechanisms, offline-compiled (13 probes, local SDE 9.13.1)

**All compile evidence, nothing run on the switch.** Four changes compile and, combined (probe P13), **fit in 11 of 12 ingress stages — one fewer than D4c uses today.**

| probe | change vs D4c | fits? | stages | critical path | extra SALUs | extra SRAM | extra hash-dist |
|---|---|---|---|---|---|---|---|
| P1 | Pkt_Tree moved earlier (parallel with Flow_Tree, not gated on it) | yes | 12 | 10 (from 11) | 0 | 0 | 0 |
| P3d | P1 + a keyed identity tag: `tag = CRC_k(src,sport,proto) + CRC_k(dst,dport,proto)` (an ADD, not XOR, so it survives the linear alias structure) | yes | 12 | 10 | +2 | +10 | +4 |
| P5 | P-b + D1's unwrapped clock | yes | 12 | 11 | 0 | 0 | 0 |
| P12 | P1 + a lease packed into the existing 16-bit result register + unwrapped clock | yes | **11** | 10 | 0 | 0 | 0 |
| **P13** | P3d + lease + unwrapped clock (everything combined) | **yes** | **11** | 10 | +2 | +10 | +4 |

**Two negative findings:** every Tofino-1 hash algorithm (IDENTITY, RANDOM, XOR8/16/32, CRC8-64, CUSTOM) is linear over GF(2) (`tofino1_base.p4:67-78`), so no hash choice alone breaks the XOR-fold alias structure — only an ADD, MIN/MAX, a table or a stateful ALU does. An atomic single-pass D4c claim ("prefer never-claimed B") does not fit one stateful ALU; recirculation is intrinsic to the design (only D4a, already failed on M1, could be atomic).

**Why P3d matters:** it is a compiled fix for the identity-aliasing weakness (see `docs/lit/symmetric_hash_aliasing.md` if written, else the XOR-fold item in `docs/results_d4c_compile.md`). An alias now needs `CRC_k(forged) = CRC_k(true)` under a *secret* key, about 2^-16 per blind attempt at the compiled 16-bit tag width (32 bits did not fit: it needs 7 hash-distribution units where stage 0 has 6). **Known bug in the probe, not yet fixed:** on a tag mismatch the slot's stored result is not forced to 0, so a failed alias still inherits the victim's stale verdict; this must be fixed before the mechanism is used.

**Why P12/P5 matter:** the unwrapped (D1) clock costs literally 0 resources at compile time, settling "D1's hardware cost is unverified" for the compiler (behaviour on the model is still owed). The lease reuses the existing result register (no new state) and gives a hardware path for the D4age family — but its semantics differ from the emulated D4age (expiry runs from the last result *write*, not from the claim time), so it needs its own emulator arm, not a relabelling of the existing one.

**Also estimated, not compiled (lower confidence):** a controller-maintained stash (exact-match, hash-free, so immune to slot-index targeting; digest-flood risk unaddressed), compare-and-set claims in pass 2 (narrows but does not remove the recirculation race), depth-1 cuckoo relocation by recirculation (blocked today by a register-action-count limit on two feature registers), a per-prefix claim-rate meter, egress offload (post-decision logic only; NetBeacon's egress is empty and shares stage memory with ingress, from memory).

**Recommended next compiles:** P3d after fixing the result-mismatch bug, and the stash design. P13 (fixes the alias, adds the lease, frees a stage, zero clock cost) is the candidate to take to the switch host with SDE 9.13.2 and the software-model diff — **needs Philip's approval; nothing has been run there.**

## P13 bug fix, follow-up (2026-09-24): the fully-correct fix costs the saved stage back

The known bug (a failed keyed-tag match still inherited the victim's stale verdict) is fixed in `p4/dgrade_d4c_p13/` (offline compile, local SDE 9.13.1 only; `p13fix_vs_p13.diff` against the original probe). **The fix costs the stage P13 had saved: 12 stages, critical path 11 (against the buggy P13's 11 stages, critical path 10) — the same stage count as D4c alone.** The root cause: gating admission itself on the keyed check (not just the returned verdict) needs the tag compare inside the same stateful ALU that decides ownership, which pushed the probe from stage 1 to stage 2 and every later table down by one.

| | Buggy P13 | Fixed P13 | D4c (frozen, evaluated design) |
|---|---|---|---|
| Ingress stages | 11 | **12** | 12 |
| Critical path | 10 | 11 | 11 |
| Stateful ALUs | 16 | 14 | 14 |
| SRAM blocks | 201 | 191 | 191 |
| Hash bits | 414 | 384 | 330 |

A cheaper, **not recommended**, 11-stage alternative exists (`p13gate_vs_p13.diff`) that only zeroes the stored result on a mismatch, without gating ownership on the keyed check: it still lets an alias refresh the victim's last-seen time (so an attacker could keep a stale slot alive indefinitely) and refuses an alias on an idle slot where a normal newcomer would be admitted. Not used, for the record only.

**Consequence for the paper track:** P13's headline ("fits in 11 stages, one fewer than D4c") does not survive its own bug fix. The corrected P13 is a same-stage, higher-SRAM/hash-cost alternative to D4c that additionally closes the aliasing weakness and adds a lease — a real but smaller improvement than first reported. It remains a separate, post-hoc implementation study (`p4/dgrade_d4c_p13/README.md`), not evidence for D4c's pre-registered claims, and is not compiled or tested beyond this local offline check.
