# Pre-registration: downgrade attacks on stateful in-network classifiers

Committed on 2026-09-23, before gate G1 runs and before any attack or defense result exists. These hypotheses, metrics and thresholds are frozen. Any later change is recorded in the change log at the end, with its date and reason, and the paper reports both the original and the amended version.

## Fixed definitions

- **Victims.**
  - **V1: NetBeacon.** The shipped PeerRush build (commit 06c6127), run in a table-level emulator from its shipped table pickles.
  - **V1′: NetBeacon-faithful retrain on CICIoT2023.** Our re-implementation of NetBeacon's phase-tree training, which we must write because none ships. It is validated against the structure of the shipped PeerRush tables before it is used.
  - **V2: BoS.** The single-pipe build (commit 38aafd9), with `T_conf`/`T_esc` re-derived under the paper's rule that at most 5% of flows escalate. The per-packet forest is retrained, because it does not ship.
  - **Control: MVM.** Never counted as a victim.
  - **Generality point: Flowrest.** It has no fallback model, so a flow without a slot gets no verdict.
- **Published provisioning.** 65,536 slots with each victim's shipped timeout, hash and admission rule, as recorded in `docs/results_g0.md`. No attack result may use a smaller table.
- **Benign concurrency.** A MAWI trace (a specific day chosen before G2, recorded here), replayed at its native rate and at the rates NetBeacon's paper used (up to 1,555 new flows/s).
- **B0.** Random spoofed new flows at the **same new-flow rate** as the attack being compared.
- **Downgraded flow.** A flow that has a verdict path in the full model but is classified, in whole or in part, by the fallback model (NetBeacon, BoS) or receives no verdict (Flowrest), because of slot contention.
- **Silent.** The drop rate changes by no more than 0.1 percentage points and p99 latency by no more than 10% against the no-attack run.
- **Detectors (fixed now).**
  - A new-flow-rate threshold per /24 source and global, Jaqen/Poseidon-style, set at the benign p99 of the chosen MAWI trace.
  - A volumetric bytes/s threshold at the benign p99.

## Hypotheses (one primary metric each)

| H | Primary metric | Pass | Falsifier |
|---|---|---|---|
| H1 gap | full minus fallback, on flows downgraded under attack. Metric per task: **macro-F1 over the degraded classes** for V1/V2 application classification; **macro-recall over attack classes** for V1′ on CICIoT2023 | ≥ 0.10 on at least one victim | < 0.03 on every victim → the fallback is "harmless" (kill criterion f′) |
| H2 amplification | benign flows downgraded per attacker packet, crafted ÷ B0 | ≥ 5× | < 2× (kill criterion d) |
| H3 stealth | ρ\* to downgrade 50% of benign flows at published provisioning, in new flows/s ÷ the victim's slot reclamation rate | both detectors silent at ρ\* | ρ\* > 1, or either detector fires (kill criterion e) |
| H4 evasion | recall on the attacker's concurrent CICIoT2023 attack flows, on V1′ | drop ≥ 20 pp | drop < 10 pp |
| H5 defense | benign macro-F1 recovered under the adaptive attacker (rate-mimicking and forging value) at the undefended ρ\* | recovers ≥ 50% of the loss, beats baselines 2–13 by ≥ 5 pp, costs ≤ 1 pp with no attack | not better than keyed hashing plus keep-heavier |

H1 is also reported, as secondary results, on (ii) the attacker's own attack classes (V1′) and (iii) each class separately, together with the benign-only downgrade rate at the chosen MAWI concurrency.

## Statistics

- 10 seeds for every stochastic element (flow sampling, spoofed sources, hash salt).
- Block bootstrap over 1-second blocks for 95% confidence intervals.
- Holm correction across H1–H5.
- Every victim configuration is frozen from its artifact before any attack run.
- Hardware runs last at least 10 minutes.

## Kill criteria

- (d) H2 falsified → drop the attack claims and pivot to a TDSC defense-only paper.
- (e) H3 falsified → same pivot.
- (f′) H1 falsified → a short negative-result note, and the defense goes into TDSC.
- (g) Neither port runs on SDE 9.13.2 within 4 weeks → emulation-only TDSC.
- (h) The defense cannot fit next to the unmodified victims (already true from G0: both use all 12 stages) → deploy it on a NetBeacon build with fewer phases, and state the reduced resource claim.
- (i) H5 fails while H1–H3 pass → an ACSAC attack paper with the defense sketched.
- Keyed hashing alone restores ≥ 80% of the loss → the attack section becomes a configuration advisory.

## Change log

- 2026-09-23: initial version.
  - H1's metric was made per task, and H4 was moved to CICIoT2023 with V1′, because G0 found that no victim dataset contains both benign traffic and attacks.
  - (h) was recorded as triggered by G0.
- 2026-09-23 (before any G1 result): the flow key was defined. The full-model reference for H1 is an isolated replay that gives each bidirectional 5-tuple its own slot and removes only contention. It does **not** split flows on idle gaps, because the switch identifies a flow by its 32-bit hash alone (`switch.p4:606`), so a tuple that resumes after a pause continues its old state. A first isolated run that split flows at 256 ms gaps was discarded for this reason: it made the reference restart flows the switch would have continued (392k takeovers against 20k under contention). Flow-level statistics use the same tuple keys.
- 2026-09-23: "downgraded" counts only refusals by an active slot holder (collisions) and later slot loss. Two other routes to the fallback are reported separately and are not counted: refusals at a never-claimed slot after a clock wrap, and flows the flow-size model predicts short. NetBeacon's design intends the latter.
- 2026-09-23 (before any G1b run): **G1b, hash comparison on benign PeerRush, NetBeacon emulator, 65,536 slots.**
  - **Hash arms.**
    1. Unkeyed CRC32, as shipped.
    2. XOR-salt CRC32 (a salt XORed into the input or init). This is a negative control. CRC is affine over GF(2), so a salt shifts every slot by one constant and does not change which tuples collide. **Prediction: the downgraded-flow set is identical to arm 1.** Any deviation means an emulator bug.
    3. Secret-polynomial CRC (a random 32-bit polynomial per draw). This changes which tuples collide and is the keyed baseline.
    4. Tabulation hash. A nonlinear reference bound, not a deployable design at 12 stages.
  - **Factors.** Hash draw crossed with clock start. Clock starts come from a fixed equally spaced grid over the 4.295 s wrap period. Unkeyed with a fixed clock is deterministic and is run once. Planned draws: 30 per arm. The 10 G1 seeds are reused for arm 1.
  - **Reference.** Each contended run is paired with an isolated reference at the **same clock start**. This is because inter-packet gaps are computed from a 32-bit timestamp that wraps every 4.295 s, so the reference depends on the clock. The G1 report compared seeds 1 to 9 against seed 0's reference, so its per-seed gaps are re-derived and superseded. Gate: the isolated run is repeated at 3 or more clock starts; if per-packet verdicts are identical, one reference suffices.
  - **Primary metric.** L = macro-F1(isolated) − macro-F1(contended) over all flows. Secondary: downgraded-packet share, the per-flow contribution to net packets lost (top 20 flows against the rest), p_big = the share of draws with L > 0.004, and the H1 downgraded-flow gap (unchanged from H1 and not used to decide neutrality).
  - **Inference.** The draw is the unit for hash and clock effects, using a percentile bootstrap over draws. Within a draw, uncertainty comes from a flow-cluster bootstrap.
  - **Neutral on benign traffic** holds iff all of: (a) the 90% CI of mean L(keyed) − mean L(unkeyed) lies within ±0.002; (b) the downgraded-packet-share difference is within ±0.1 pp; (c) the p_big intervals overlap. Otherwise the changed distribution is reported as is. If the intervals are too wide, the report says "not shown neutral".
  - **Not evaluated here:** the kill rule "keyed hashing alone restores ≥ 80% of the loss". It needs the attack arms in G2, including a targeted-displacement arm (known hash against secret polynomial).
  - **Uncertainty carried by all G1 and G1b counts:** the emulator hashes the sorted 5-tuple, while the switch may build `@symmetric` from a different construction. This is checked on the SDE model before any collision count is used in the paper.
- 2026-09-23 (after independent code review, after a partial G1b dry run of 19 draws per arm had been viewed): **amendments to G1b.** They are recorded here because they postdate partial results. None was chosen to favour a hypothesis; each closes a gap between the text above and what was run.
  1. **Downgrade definition.** A flow that held a slot, lost it, and is later answered as "predicted short" is also counted as downgraded (the review measured 61 to 66 such flows per run, about 7.5% of the downgraded set). This is the "later slot loss" of the earlier entry. Counts are recomputed from the saved runs; no run was repeated.
  2. **Verdict logic.** Each neutrality rule is pass, fail (interval entirely outside the bound) or inconclusive. Any fail reads "changed distribution"; "not shown neutral" is reserved for inconclusive intervals, as the addendum states.
  3. **Polynomial arm split.** The 32-bit random polynomials drawn for arm 3 are mostly reducible (the review found 1 irreducible of 30 seeds), and keyed-CRC collision bounds assume irreducibility. Arm 3 is kept as **random polynomial (as run)**, and a new arm **3b, irreducible polynomial**, is added with 30 draws per column, chosen by rejection sampling and checked against sympy. The keyed baseline for neutrality and for G2 is 3b; the other arm is reported beside it.
  4. **Clock design.** The design is a diagonal (draw g at clock g) plus one fixed-clock column (30 keys at grid 0), not a full salts × clocks cross. The 10 G1 seeds are not reused; the clock grid replaces them. A draw with key 0 at grid 0 belongs to both columns. The tabulation hash has a clock-varied column only.
  5. **Attribution.** "Top 20 flows" means the 20 largest flows by packet count; the five largest contributors to the loss are listed for the largest-L run.
  6. **Within-draw uncertainty.** The flow-cluster bootstrap is reported for the grid-0 draw of each arm, not for every draw.
  7. **Reference gate.** Reported in the results document: references differ by 134 to 2,145 of 4.85 M packets (the first draft of this line said 135) across clock starts, so all comparisons are paired.
- 2026-09-23 (before any G2 data is downloaded or opened): **G2 benign traffic is fixed.**
  - **Source:** MAWI Working Group Traffic Archive, samplepoint F (Tokyo transit link), the 14:00 to 14:15 daily capture. Headers only (96-byte capture length). Research use only.
  - **Primary day:** Wednesday 2022-09-14 (`samplepoint-F/2022/202209141400.pcap.gz`; 125,402,386 packets, 8,662.83 MB raw, 2,847.89 MB gzipped).
  - **Replication day, chosen by a mechanical rule** (exactly 26 weeks later, same weekday, still before the August 2023 change that added a 10 Gbps link to the capture point): Wednesday 2023-03-15 (`samplepoint-F/2023/202303151400.pcap.gz`; 108,803,846 packets, 7,902.78 MB raw, 2,917.26 MB gzipped).
  - **Selection basis:** the primary day was chosen on stated criteria (mid-week, not a Japanese public holiday, before the 2023 link change, high packet count), not by a mechanical rule and not from looking at its contents. Neither trace had been opened when this entry was written.
  - **Slice:** the first 120 seconds of each trace, taken as the first 120.0 s after the first packet's timestamp. This is fixed in advance to keep the emulator's run time practical and covers about 28 clock-wrap periods and 450 idle timeouts. The gzip file is downloaded only in part (the first 500,000,000 bytes) and cut at the last whole packet.
  - **Filter:** the same parser filter as the PeerRush loader (IPv4 without options or fragmentation, TCP or UDP).
  - **Detectors:** the new-flow-rate and volumetric thresholds are set at the benign p99 of the primary-day slice, measured in 1-second bins.
  - **Reporting:** every G2 number is reported on both days. A result that holds on only one day is reported as such.
- 2026-09-23 (after four single-clock MAWI runs had been viewed; before the grid below): **G2a, the benign baseline on MAWI.**
  - **Runner:** `scripts/g2_mawi.py`. Two flow-size gates per configuration: `model` (the shipped PeerRush-trained flow-size model) and `oracle` (a flow may take a slot iff it truly has more than 50 packets in the slice, NetBeacon's long-flow definition). The gate mismatch is treated as a design factor, not a caveat: `model` is the worst case for occupancy, `oracle` a deployment-appropriate case.
  - **Runs (both days, both gates):** unkeyed CRC at 30 clock starts (grid 0 to 29); irreducible-polynomial CRC with keys 0 to 29 at clock grid 0. That is 240 runs; the four single-clock runs already made are grid-0 members of the set.
  - **Metrics (no accuracy labels exist on MAWI):** downgraded-flow share and downgraded-packet share (definition as in G1b: refused by an active holder, or displaced after holding a slot); refusals at never-used slots reported separately; slot takeovers; memo share. Reported per day and per gate.
  - **Uncertainty:** the draw is the unit (30 clocks, 30 keys); intervals by percentile bootstrap over draws.
  - **Use:** the reference against which G2b attack arms are measured, and the benign new-flow and byte rates already fixed above set the detector thresholds.
- 2026-09-23 (G2a amendment, before the G2a grid finished): a single MAWI run peaks at about 7.4 GB (about 1.7 GB early in the run), which capped parallelism at 3 and stalled the first launch of 240 runs. The **oracle** gate has about 10^4 takeovers and is light; the **model** gate has about 10^6 and is heavy. Oracle is the primary gate (smaller benign baseline, deployment-appropriate occupancy, conservative for the attacker). **The model-gate grid is reduced to 10 draws per arm** (clock grid 0 to 9; keys 0 to 9). The oracle grid stays at 30 draws per arm. The four single-clock runs made earlier are grid-0 members.
- 2026-09-23 (before any G2b run; G2a viewed on 4 runs only): **G2b, attack arms on MAWI, NetBeacon emulator.** Merges a statistician review and a network-realism review.
  - **Abstraction (analytical, emulator only).** K1 (hash-knowing adversary) is *given* attacker-chosen slot indices; no hash search or crafted packets are modelled, and the CRC's affine structure is unit-tested to show that such an adversary could in principle choose them. K0 (secret hash) gets uniformly random slots. Attacker flows send one packet per 250 ms (sensitivity: 268 ms), start at a random phase relative to the 4.295 s wrap, are treated as long by construction, and are rotated before 2048 packets. Rotation cost is computed analytically: the 120 s slice is shorter than one rotation (about 549 s), so no rotation occurs in the slice. Whether a first-packet pattern passes the shipped model gate is checked; if it does not, the claim is restricted to the oracle gate.
  - **Arms, both days, oracle gate primary and model gate secondary:**
    - Fill fractions f = 1, 5, 10, 25, 50, 75, 90% of 65,536 slots, K1 and K0. Predicted: K1 and K0 differ by about f/2 in relative terms, so the fill arms do not test hash knowledge.
    - Targeted displacement of the 20 largest benign long flows: K1-preempt (the adversary holds the victim's slot before the victim starts, an oracle upper bound), K1-reactive (waits for a victim gap over 268 ms), and K0. **The keyed-hash rule is judged on the targeted arms only.**
    - Three deployment cases reported separately: unfiltered (spoofed) edge, filtered edge with a per-source cap on concurrent flows and packets per second, compromised insider.
  - **Baselines.** B0-s (random short flows at the same new-flow rate) is reported but not decisive, because short flows never take slots. **B0-L (random long-predicted flows at the same packet rate) is the decision baseline.** K0-crafted separates gate-gaming from hash knowledge.
  - **Metrics.** Excess downgraded flows and packets over the paired no-attack run (same clock start, same hash draw, same benign stream), realised occupancy alongside nominal f. Warm-up bins 0 to 4 are excluded.
  - **H2** = excess benign flows downgraded per attacker packet, crafted over B0-L. Pass if the lower 95% bound is at least 5, falsified if the upper bound is below 2, otherwise inconclusive.
  - **H3** = ρ*, the attacker's sustained new-flow rate at the smallest f whose excess is at least 50% of benign long flows, divided by R = 65,536 / 0.268 s ≈ 2.45 × 10^5 flows/s. Pass if ρ* ≤ 1 **and** the detectors are silent. Detectors: new flows/s and bytes/s at the frozen benign p99 of 1-second bins, plus packets/s and new flows per /24, with threshold sensitivity at p50, p90, p99 and p99.9 and the false-alarm rate reported. Passing against volumetric and new-flow detectors is expected to be trivial (about 0.1% of the new-flow p99, a few percent of bytes) and is reported as such; the packet rate and a low-rate regularity signature are the informative axes. Drop rate and latency silence cannot be measured in an emulator and are labelled so.
  - **Decision.** The oracle gate on the primary day decides; the replication day and the model gate are reported alongside. A result that holds on one day only is reported as such.
  - **Statistics.** Diagonal design (draw g at clock g), 30 draws per arm and day for oracle, 10 for the model gate, a separate K0 slot draw per g. The draw is the bootstrap unit (percentile bootstrap of the ratio of means). For targeted arms the 20 victims are cases, reported per victim with the draw as the unit. Holm correction across H1 to H5.
  - **External validity.** MAWI-F is a stress case (about 6x the new-flow rate of PeerRush), not a proxy for a campus deployment. Results are also reported against a dimensionless load, slots per new-flow per second, by thinning MAWI flows and by resizing the table, so the trend can be read across load levels.
  - **Robustness arms** for the behaviours inferred from code and never observed on hardware: each of the clock-wrap eviction window, takeover-without-timestamp-refresh and the recirculated-copy write is switched off in turn, and the headline results are reported under both settings. Occupancy is checked against Little's law and the arithmetic above.
  - **Validation still owed (needs Philip's approval before it touches the switch host):** run the NetBeacon P4 on the SDE software model with a short trace and diff its per-packet verdicts against the emulator.
- 2026-09-23 (G2a amendment, before the grid finished): per-day precomputation (distinct tuples, oracle flags, the shipped model's per-packet lookups) cut a MAWI run from about 2 minutes to 19 to 35 seconds. Two reruns of runs made before the change gave **identical per-packet outcomes**. The memory cap (about 7 GB per run, so parallelism 3) is unchanged, but the time no longer forces a smaller grid, so **the model gate is restored to 30 draws per arm**, as originally pre-registered. The 10-draw reduction entered above is withdrawn.
- 2026-09-23 (G2b clarification, after a one-draw pilot of the fill arms had been viewed, before the full grid): **which arms decide H2.** The pilot (primary day, oracle gate, clock 0) gave excess benign flows per attacker packet of 224.7 × 10⁻⁶ for K1 at 10% fill against 217.6 × 10⁻⁶ for B0-L, about 1.03×. This confirms the prediction already recorded in the G2b entry that fill arms do not test hash knowledge. Philip decided (2026-09-23) that **the targeted-displacement arms decide H2 and kill criterion (d)**. The fill arms' H2 ratio is reported prominently and is expected to be falsified (below 2×); that result is a finding (a uniform fill gains nothing from slot knowledge; the damage comes from holding long-lived slots), not a hidden outcome. The pilot is one draw and is not used for any decision. The keyed-hash rule remains judged on the targeted arms.
- 2026-09-23 (G2b amendment, compute budget, before the full grid; the pilot showed excess is near-linear in the fill fraction): the fill-arm draw counts are reduced. **Oracle gate:** 30 draws at f = 1, 10 and 50%; 10 draws at f = 5, 25, 75 and 90%. **Model gate:** 10 draws at f = 1, 10 and 50% only. All three modes (k1, b0l, k0), both days. That is 960 runs. The per-run memory is about 3.8 GB for f up to 10% and about 8.5 GB at f = 90%, so jobs run in ascending order of f with lower parallelism at large f.
- 2026-09-23 (G2b targeted-arm design, before any targeted run; only flow-size statistics of the two slices were computed): **victim sets and arm definitions.**
  - **Why the literal victim set needs a companion.** The 20 largest benign long flows carry 48,302 to 1,226,235 packets each on the primary day (34.6% of all packets) and 59,344 to 315,130 on the replication day (22.0%). Each reaches 2,048 packets within a fraction of a second and is then marked determined and answered from the verdict memo whatever its slot holds. A holder that pre-empts such a flow's slot keeps it from ever being determined, so all of its packets stay on the fallback (a large packet-weighted effect); a reactive holder can only displace it during its first 2,048 packets.
  - **Two victim sets, both reported.** (A) the 20 largest benign long flows (the entry above, literal); (B) the 20 largest benign flows that never reach 2,048 packets in the slice (1,850 to 2,045 packets on the primary day, 1,977 to 2,046 on the replication day), which are always exposed to the slot logic.
  - **Arms (oracle gate, both days, draw g at clock g, 30 draws).** K1-preempt: one holder per victim, given the victim's slot, started 500 ms before the victim's first packet (at time 0 if earlier). K1-reactive: one holder per victim, given the victim's slot, started 275 ms after the end of the victim's first inter-packet gap longer than 300 ms; a victim with no such gap is not attacked and is counted as an attack failure. Random holders (B0-L, benign hash unkeyed) and K0 (benign hash keyed, holders on random slots), each with H = 20, 200, 2,000 and 20,000 holders started in a 5 s ramp, giving the cost curve against the K1 cost of 20 holders. Every holder sends one packet per 250 ms until the slice ends.
  - **Metrics.** Victims displaced (flagged as downgraded in the attack run and not in the paired no-attack run) out of 20, victim packets downgraded, and attacker packets. H2 = victims displaced per attacker packet, K1 over B0-L at the same number of attacker packets. The random arms are also reported at equal attacker packets against K1.
  - **Expected (recorded before running):** K0 and B0-L with H = 20 hit about 20 × 20 / 65,536 = 0.006 victims; H = 20,000 about 6 of 20. K1 hits close to all 20 in the preempt arm.
- 2026-09-23 (G2b amendment, before the full targeted grid; the pilot showed B0-L at 20 holders displaces 0 of 20 victims): **H2 on the targeted arms is a rate difference, not a ratio.** With H = 20 random holders against 65,536 slots, the expected number of victims hit by chance is 20 × 20 / 65,536 ≈ 0.006, so B0-L's displaced-victim rate is expected to be zero in almost every draw and a ratio against it is undefined or explodes. **H2 is redefined for the targeted arms as the difference in victims displaced per attacker packet, K1-preempt minus B0-L (20 holders), with a 95% CI from a percentile bootstrap over draws.** Pass if the lower bound exceeds 0 by at least 5× B0-L's own upper 95% bound (equivalently, K1 displaces at least 5× as many victims as the largest plausible B0-L rate); falsified if K1's upper bound is below 2× that same B0-L bound. The ratio form in the earlier G2b entry is retained for the fill arms, where both terms are comfortably nonzero, and is reported for the targeted arms only as a secondary, labelled undefined where the denominator is zero.
- 2026-09-23 (G2b amendment, before the full targeted grid; from the one-draw pilot): **pre-empting holders may start before the slice.** The slice begins mid-capture, so a flow already in progress at t = 0 has its first packet at t ≈ 0 and a holder clipped to start at t = 0 loses the tie. A pre-empting adversary models one that was already holding when the observation window opened, so the holder starts 500 ms before the victim's first packet even when that is before t = 0 (negative timestamps; the emulator's clock arithmetic is modular). Holder packets before t = 0 count towards the attacker's cost. Without this the pilot's top-victim pre-empt arm displaced only 8 of 20 victims for a reason that is an artefact of slice truncation. The "vulnerable" victim set (flows of 1,850 to 2,046 packets) is not affected. **Report additions:** victims already downgraded at baseline (out of 20), and the secondary count of victims with at least half of their packets answered off the stored state.
- 2026-09-23 (G2b scope decision, Philip): **the targeted-displacement grid is paused and its report extension is not built.** Committed and tested: the victim-selection and holder module (`inject_targeted`), the targeted runner, the one-draw pilot design, and the H2 rate-difference definition above. No targeted result is reported beyond the one-draw pilot, which is not used for any decision. The fill arms and the G2a baseline continue. **Consequence for the hypotheses:** H2 has no decisive arm and stays open; kill criterion (d) is not evaluated. The paper's statement on steering is limited to (i) the analytic expected-cost result (about 1 flow with slot knowledge against about 65,536 without, per chosen slot) and (ii) the fill-arm finding that a uniform fill gains nothing from slot knowledge. The defence claims (keyed hash, wrap-window fix, rent admission) are evaluated in G4 and do not depend on the targeted arms.
- 2026-09-23 (before any G4 run; merges a statistician design review and a Tofino feasibility review): **G4, defence evaluation in the emulator.**
  - **Amends H5 for the emulator.** MAWI has no labels, so loss is measured as benign downgraded flows and packets (oracle gate) and PeerRush supplies macro-F1 for benign cost. "The undefended ρ*" is replaced by an equal attacker packet budget at fill fractions f = 10% and 25%. The clause "beats baselines 2 to 13" and the keyed-hash kill rule ("restores at least 80% of the loss") are **not evaluated in G4** (the latter is judged on the paused targeted arms and stays open). The lemma is restated as attacker cost per held slot-second (at least r_min), with spray excluded.
  - **Defences (emulator switches).**
    - **D1, wrap fix:** `wrap_window=False`, `takeover_refresh=True`, and `fix_ipd_wrap=True` (the packet-gap timestamps get the same precedence fix, which changes the gaps the trees see; the PeerRush accuracy check measures that).
    - **D2, secret irreducible-polynomial CRC** (already in the emulator).
    - **D3, credit-based rent admission (the deployable variant).** At takeover the slot records `t0 = now` and a credit horizon `V = now + 1 s`; each packet of the incumbent adds `1 / r_min` seconds to V, capped at `now + 2 s`. A new long-predicted flow evicts an undetermined incumbent iff `now - t0 >= 1 s` and `now > V`. Requires D1's clock. r_min in {4 (control: equal to the attacker's own rate, not a defence), 8, 16, 32}.
    - **D3-age:** evictable after 1 s regardless of rate (shows whether rate discrimination matters). **D3L:** the literal cumulative-rate predicate (`packets / age < r_min`), emulator-only, not deployable in one pass.
    - Combinations at r*, the largest r_min that passes M1 (chosen without looking at attack results, same rule on both days).
  - **Deployability claims.** D1 and D2 may be described as deployable only after the compile-only check (a separate build directory, no `bf_switchd` restart, needs Philip's approval on the switch host). **D3 is claimed as an emulator result** unless its credit-register action compiles within 12 stages.
  - **G4a core arms (first stage; the rest is staged later).** Configurations D1, D2, D3 at r_min 8, 16, 32, D3-age against the undefended runs already in the fill grid. Attacks: none, and fill at f = 10% and 25% with random-slot holders (b0l, benign hash unkeyed) at 4 packets per second, paired with the fill-grid runs of the same draw. Secondary, later: the r_min-mimicking adversary at equal budget, burst-then-trickle, rotating holders, and K0 spray costed at 1 and at 51 packets.
  - **Metrics and rules (oracle gate; the primary day decides; the replication day must agree in sign; Holm across r_min).**
    - **M1, benign cost with no attack:** macro-F1 change against the paired unkeyed run on PeerRush, and downgraded-packet-share change on MAWI. Pass if the 95% upper bound is at most 0.01 and at most 0.5 percentage points.
    - **M2, recovery under attack at equal attacker packets:** 1 - (defended attack minus undefended no-attack) / (undefended attack minus undefended no-attack), on excess benign downgraded flows. Pass if the 95% lower bound is at least 0.5 at both f.
    - **M3, attacker cost:** attacker packets per held slot-second at least 0.9 × r_min, and cost per excess benign downgraded flow at equal budget at least 0.5 × (r_min / 4) times the undefended value at f = 10%.
    - H5-G4 passes for a configuration iff M1, M2 and M3 pass; falsified if M2's upper bound is below 0.25 for every r_min or M1 fails for every r_min; otherwise inconclusive.
  - **Predictions (before running).** D2 leaves the fill excess within ±10% of undefended. D1 removes at least 90% of empty-slot refusals (and may not lower fill excess, since the wrap window is currently an extra eviction chance against holders; this is tested two-sided). D3-age costs more benign downgrade than D3. D3 at r_min = 4 recovers about nothing.
  - **Design.** 30 draws (draw g at clock g) on the primary day at f = 10% and with no attack; 10 draws at f = 25% and on the replication day. Percentile bootstrap over draws of paired ratios of means. Holders rotate before 2,047 packets; the 120 s slice is shorter than the determination time at r ≤ 16, so long-horizon holding is analytic.
  - **Reports.** The rate distribution of oracle-long incumbents at ages 1, 5 and 30 s and the share below each r_min (slow benign flows exposed to eviction); PeerRush and MAWI are never pooled; the SDE-model diff and the compile-only stage-fit check remain owed and are not tested by G4.
- 2026-09-23 (G2b amendment, before the large-fill runs; evidence: at f = 10% and 25% the keyed-hash arm (k0) matched B0-L to within 1 to 2% on both days, see `docs/results_g4a.md`, D2 section): **the f = 50, 75 and 90% fill arms are reduced to the B0-L mode only, oracle gate, 10 draws per fraction and day** (60 runs). The k1 and k0 modes at those fractions are not run; the fill arms do not test hash knowledge (predicted, and confirmed at f = 10 and 25%). Purpose: H3, the smallest fill fraction at which the attack downgrades at least 50% of benign long flows, and the attacker rate that implies against the pre-registered detector thresholds. The model-gate fill arms and the f = 1, 5% oracle arms beyond the runs already made are not run.
- 2026-09-23 (before any G5 run; already viewed: the G4 results, and the retrospective full-vs-fallback agreement table in `docs/results_v2_agreement.md`, PeerRush isolated run): **G5, value-aware admission.** Merges a statistician design, a Tofino feasibility review and an adversarial review.
  - **Motivation.** Rent recovered 0.11 to 0.18 flow-weighted because rate cannot separate a 4 packet/s holder from benign long flows (median 2.7 to 3.7 packets/s). The discriminator must come from something other than rate.
  - **Correction to two earlier premises.** The +0.035 full-versus-fallback gap belongs to the flows downgraded under benign load (about 1% of flows). Across the whole PeerRush stream the full model scores macro-F1 0.842 and the per-packet fallback alone 0.530, so slot protection matters a great deal, and the expected loss under a fill attack is of order f × 0.3 (about 0.03 at f = 10%, 0.08 at f = 25%), not f × 0.035.
  - **What retrospective agreement suggests (viewed).** Flows whose full and fallback verdicts agree on at least 95% of their packets are 44% of long flows and 48% of packets; giving them the fallback costs 0.0020 macro-F1. That is an upper bound for a rule that only sees phase-packet verdicts so far.
  - **Arms.**
    - **V2-evict:** an undetermined incumbent that has reached at least two phase packets and whose full-model phase verdict agreed with the per-packet fallback verdict at every phase reached so far is evictable after a 1 s grace, by any new long-predicted flow. Non-sticky in the sense that a later disagreement restores protection.
    - **V2-credit:** as D3 credit at r_min = 16, with weight 2 on the credit for a packet of a flow that has disagreed at some phase and weight 0 for a flow that has always agreed.
    - **Oracle-V (ceiling, PeerRush only):** an incumbent is evictable iff its true remaining gain (sum over its remaining packets of [full verdict correct] minus [fallback verdict correct]) is at most zero.
    - **Baselines:** undefended, D1, D3 credit at 8 and 16.
    - **Not included:** V1 (first-packet value; attacker-set), V3 to V5, and the two-way associative comparator (the latter awaits Philip's decision).
  - **G5-0 gate (isolated PeerRush, no attack; run before any attack run).** Report the phase agreement rate, persistence (agreement at the next phase given agreement so far), the share of slot-seconds held by agree-so-far flows, and the oracle-evict macro-F1 cost when every agree-so-far flow is forced to the fallback after phase 4 and after phase 8. **Kill V2-evict and V2-credit if** the cost's upper 95% bound exceeds 0.01, **or** the slot-second share of agree-so-far flows is below 20%.
  - **Forgeability p_f (reported, abstract).** The trees are public, so a holder that makes its phase verdict disagree with the per-packet verdict earns protection. p_f is the share of holder-feasible packet-size patterns at 4 packets/s whose phase and per-packet verdicts disagree; it is estimated by sampling the shipped trees offline, and no traffic is generated. If p_f ≥ 0.05, forging is cheap and the attacker-cost claim for V2 is dropped (value is then a recovery-of-wasted-slots claim only).
  - **Attacks (PeerRush, holders injected into the benign stream, oracle abstraction as in G2b).** Fill at f = 10 and 25%, sensitivity f = 50%, holders at 4 packets/s. Two adaptive arms at the same attacker packet budget: value-forging holders (the emulator marks every holder flow as disagreeing at each phase; an upper bound that does not price the search for such packets, which is p_f) and spray (many long-predicted flows that each send only the grace-period packets). The rate-mimicking holder is covered by D3.
  - **Metrics and rules (primary: PeerRush macro-F1; MAWI reports flow and packet occupancy only, with no value model applied).**
    - **Power gate:** the undefended fill's macro-F1 loss must have a 95% lower bound of at least 0.004; otherwise the result at that f is "not tested".
    - **M1:** benign cost with no attack, macro-F1 change upper 95% bound at most 0.01; on MAWI, downgraded-packet-share change upper bound at most 0.5 percentage points.
    - **M2:** macro-F1 recovery = 1 - (defended loss) / (undefended loss) against the paired undefended no-attack run; pass if the lower 95% bound is at least 0.5 at f = 25%; falsified if the upper bound is below 0.25 for every arm.
    - **Adaptive:** defended loss at equal attacker packets must not exceed the undefended loss for the forging and spray arms; exceeding it is reported as a failure of the defence.
    - **Kill:** if Oracle-V fails M2, the value direction is closed and reported as such.
  - **Design.** 30 draws (draw g at clock g) at f = 10%, 10 elsewhere; paired references (isolated PeerRush run at the same clock); percentile bootstrap over draws, with a benign-flow-cluster bootstrap alongside because all draws share one capture; Holm across arms.
  - **Emulator caveat, stated in advance.** V2 needs the phase verdict at the moment an eviction decision is taken; the emulator evaluates phase verdicts with a fast tree evaluator equal to the table evaluation (fidelity-tested), so no delay approximation is used. The one-pass Tofino build (a per-slot agreement register written on the existing phase recirculation) is unverified; the compile-only check and the SDE-model diff remain owed and are not tested by G5.
- 2026-09-23 (G5-0 gate result, `docs/results_g5_gate.md`): **V2-evict and V2-credit are killed by the pre-registered rule.** Forcing flows that agreed with the fallback at every phase reached (at least two phases) onto the fallback costs 0.0368 macro-F1 after phase 4 and 0.0128 after phase 8 (limit 0.01), identically at six clock starts, so the interval adds nothing. The slot-second share of agree-so-far flows is 27% (above the 20% floor) and persistence of agreement is only 71 to 76%. The earlier whole-flow retrospective table (cost 0.0020) used hindsight a switch does not have. **Consequence:** no value-term rule built on phase-verdict agreement proceeds to the attack runs. Oracle-V, the labelled ceiling, has not been run; the pre-registered kill test ("Oracle-V fails M2") stays open and is only informative as a ceiling. The undefended fill attack on PeerRush, which measures the labelled accuracy harm and gives the power gate, is still run.
- 2026-09-23 (G5 amendment: the labelled fill attack on PeerRush; only benign-concurrency statistics viewed): **load-matched table and run sizes.**
  - **Why a smaller table.** PeerRush has 1,102 long flows with about 500 alive at once (median; 10 s samples), which is 0.8% of a 65,536-slot table, against 5.6 to 6.7% for the two MAWI slices (median 3,652 to 4,383 alive). A fill of 10% of a 65,536-slot table would rarely touch a PeerRush flow, so the experiment would measure nothing. **The PeerRush attack experiment uses 8,192 slots**, which gives the benign load about 6% occupancy, matching MAWI. Holders and baselines use the same 8,192-slot table; the isolated (full-model) reference is unchanged by table size.
  - **Holders.** The PeerRush stream lasts one hour, so holders rotate: each rotation window lasts 490 s and every holder stops before 2,048 packets (500 s); holders in each window take random slots (B0-L style). Fill fraction f is of 8,192 slots. Holder cost at f = 10% is about 12 million attacker packets.
  - **Arms and draws.** No-attack baselines at 8,192 slots (30 clock starts); undefended fill at f = 10% (30 draws) and f = 25% (10 draws); defended arms D1, D3 credit at r_min = 8 and 16 at f = 10% (10 draws at clock starts 0, 3, ..., 27) and f = 25% (5 draws). This replaces the 30 and 10 draws of the G5 entry for the defended arms (compute: each run replays 17 to 34 million packets); f = 50% is not run. Metrics and decision rules are those of the G5 entry, with macro-F1 loss measured as macro-F1 of the paired isolated reference minus macro-F1 of the contended benign packets, at the same clock start.
  - **No claim about a 65,536-slot PeerRush deployment** is made; the load-matched table is a design choice stated here.
- 2026-09-24 (post-hoc corrections after an outside review of the pushed repository; every item below was decided after the results named in it were seen):
  - **Detector analysis (H3).** The earlier test compared mean benign packets per second plus the attacker's estimated rate with a frozen benign p99. It did not count alarming bins. `scripts/g2b_h3_report.py` now compares each one-second bin (benign count plus the attacker's count) with the thresholds frozen on the primary day and reports observed alarm rates, benign-only alarm rates on both days, and the start-up transient separately. "Detectors stay silent" is withdrawn; the packet-rate detector (a volumetric detector) alarms in 35% of bins at 10% nominal load and in essentially every bin from 25% on the primary day. The new-flow-rate detector stays at baseline; the byte-rate detector rises modestly from a baseline that is already 9.6% on the replication day.
  - **Nominal load versus occupancy.** The random-slot fill draws slots with replacement, so a nominal holder load f targets 1 − exp(−f) distinct slots (about 56% at f = 0.83). The reports now state both. The generator is unchanged (the pre-registered K0 model).
  - **G5 launcher.** The first launcher paired defended 25% runs at clocks 12, 18 and 24 with no undefended run, and used parallelism 4 and 2 (the run was OOM-killed). The corrected launcher (`scripts/run_g5.sh`) checks pairing before running, adds no-attack runs of every defence at 8,192 slots (D1's own baseline and D3's M1 cost), and uses parallelism 2 and 1. Undefended draws at f = 25% were extended to clocks 12, 18 and 24.
  - **PeerRush grid result (`docs/results_g5_peerrush.md`).** Undefended excess loss is +0.0065 at 10% (30 draws) and +0.0179 at 25% (13 draws), resolved by both the draw and benign-flow intervals. D1 improves no-attack accuracy (+0.0020) but has negative recovery under fill (−0.88 at 10%, −0.60 at 25%); D3 costs 0.0100 to 0.0108 with no attack (upper bound over the 0.01 M1 limit) and has negative recovery. The pre-registered M2 fails for D1, D3 credit r_min 8 and 16 on PeerRush.
  - **D1 components (post-hoc diagnostic).** D1 was split into no wrap window (`d1w`), takeover refresh (`d1t`) and packet-gap timestamp (`d1i`), at f = 0 and 10% on clock starts 0, 3, 6, 9 (point means). The no-wrap-window change alone accounts for the increase in the fill's excess (+0.0062 undefended, +0.0219 `d1w`, +0.0221 `d1`). The MAWI D1 result (small positive recovery) has not been re-run with this split. "D1 is free" is withdrawn; hardware cost and switch equivalence stay unverified.
- 2026-09-24 (G6, before any D4, D5 or D4s run on the PeerRush or MAWI data; already viewed: `results_g4a.md`, `results_g5_peerrush.md`, `results_g4a_d1parts.md`, `REPORT.md`; the only D4 outputs seen are synthetic unit-test streams and an independent QA probe at 4,096 slots): **capacity-matched two-choice tables.** Drafted by the statistician agent, adjusted by the PI.
  - **Designs (frozen).** **D4:** the same total slot count N split into two tables of N/2 (PeerRush 4,096 + 4,096; MAWI 32,768 + 32,768). Table A is indexed by the shipped CRC32; table B by a second fixed irreducible-polynomial CRC (seed 7919). A packet uses the slot where its identity is resident. A newcomer takes a takeable candidate (empty, idle beyond the timeout, or determined), preferring a never-claimed slot, then the longest idle, and is refused only when both candidates are held. The shipped clock behaviour is kept (D1's changes are not applied). **D5:** D4 with table B's polynomial drawn per draw (seed 900000 + clock start); on fill arms it is predicted equal to D4 (D2 showed keying leaves fill excess unchanged), so its benefit is analytic against a hash-knowing attacker and is not tested here. **D4s (control):** table B uses table A's index, a 2-way set-associative table; if D4 does not beat D4s with a resolved interval, the claim is "two-choice at equal capacity", not "independent hashes". Holders have one uniform slot in each half (analytical abstraction as in G2b); their identities and start times equal the one-table draw with the same seed, their slots do not.
  - **Definitions.** For arm X, L_X0 is the no-attack loss and L_Xf the loss under fill at the same clock start; E_X = L_Xf − L_X0. MAWI loss = benign flows downgraded (oracle gate); PeerRush loss = macro-F1 of the isolated reference minus macro-F1 of the contended packets. **Benign gain** G_X = L_und0 − L_X0 is reported in its own column and is never credited to any pass. **Mitigation** = E_und − E_X. **Recovery** R = 1 − mean(E_X) / mean(E_und), paired by draw. The G4a and G5 ratio (defended attack against the undefended no-attack run) is shown beside it for continuity and decides nothing. The mechanism outcome, **collision-refusal share**, is the share of benign packets refused by an active holder or benign incumbent (outcome code FALLBACK_COLLISION).
  - **Hypotheses.** **H-D4-mech (primary):** on MAWI, primary day, oracle gate, B0-L holders at 4 packets/s, f = 10%, 30 draws, the lower 95% bound of R on excess downgraded flows is at least 0.5; falsified if the upper bound is below 0.25. **H-D4-acc (co-primary):** PeerRush, 8,192 slots, f = 25%, 30 draws, excess macro-F1: **strong pass** if the lower bound of R is at least 0.5, **weak pass** if the lower bound of mitigation is above 0 with the lower bound of R below 0.5 (worded "resolved positive, at-least-half not resolved"), **falsified** if the upper bound of R is below 0.25, otherwise inconclusive. This three-level verdict replaces the single M2 bar for D4 because at 30 draws only a true recovery of about 0.86 could be certified as at least half (power to clear 0.5 at the predicted 0.66 is about 0.19); the deviation from G5's M2 is recorded here. PeerRush f = 10% macro-F1 is descriptive (the undefended excess narrowly fails the G5 power gate: lower bound 0.0039 against 0.004). **H-D5:** on MAWI, f = 10%, 30 draws, E_D5 / E_D4 has a 90% interval inside [0.9, 1.1]. **M1:** PeerRush no-attack loss increase at most 0.01 (upper bound); MAWI downgraded-packet share change at most 0.5 pp; benign gain gives no credit elsewhere. **M3 (matched refusal):** the nominal load at which D4's collision-refusal share equals the undefended share at f = 10% (interpolated between f = 10, 25, 50%), reported as a holder-count multiple; pass if the lower bound is at least 2.
  - **Defence-level rule.** D4 passes only if H-D4-mech passes, H-D4-acc is at least a weak pass, M1 passes and M3 passes; stated as an emulator result. Mech strong pass with accuracy inconclusive is worded "reduces refusals; accuracy benefit not resolved at 30 draws"; mech fail closes D4 as a negative result. No threshold, arm or design is changed after a look; a further variant (three choices) needs a new dated entry.
  - **Predictions (model-derived, recorded before running).** At benign occupancy about 6% and distinct holder occupancy q = 1 − exp(−f): recovery of the excess about 0.78 at f = 10% and about 0.66 at f = 25% (the adversary model with two-choice holders gives collision-refusal shares of 0.025 against 0.15 at f = 10%, and 0.090 against 0.27 at f = 25%); no-attack collision-refusal share falls several-fold (a synthetic QA probe measured 1.48% to 0.40%); matched-refusal budget multiple about 2 to 3; D5 ≈ D4 and D4s ≈ D4 on fill arms (within ±0.1 in R). The benefit is expected to shrink and reverse at high benign load (near 2·q_b + q_h = 1), so a load sweep is required.
  - **Arms and draws.** **PeerRush (8,192 slots):** D4 no-attack, f = 10% and f = 25%, 30 draws each (clock starts 0 to 29); the undefended f = 25% arm is extended from 13 to 30 draws so every D4 draw has a partner; D4s at f = 0, 10, 25% with 10 draws (clock starts 0, 3, …, 27); D5 at f = 0 and 10% with 10 draws. **MAWI (65,536 slots, oracle gate):** primary day D4, D5 and D4s at f = 0 and 10%, 30 draws each; D4 at f = 25% and 50%, 10 draws; replication day D4 at f = 0, 10 and 25%, 10 draws (must agree in sign with lower bound of R above 0). **Load sweep (MAWI primary day, D4 and undefended, f = 10%, 10 draws):** table sizes 32,768, 16,384 and 8,192 slots (benign occupancy about 12%, 25%, 50%); the claim is limited to the load range where R > 0. Attacker arms: **equal-budget** (same packets) for every defended arm, plus the **matched-refusal** arm above. PeerRush and MAWI are never pooled.
  - **Intervals and multiplicity.** Percentile bootstrap over draws, plus a benign-flow-cluster bootstrap beside every PeerRush draw interval (the flow-cluster interval for mitigation must exclude zero or the accuracy verdict drops one level). A paired sign-flip test decides ties at n = 30. Holm across the four elementary hypotheses (D4-mech, D4-acc, D5-mech via the same rule, D5 equivalence); everything else is a labelled secondary family.
  - **Required checks (reported, not exploratory).** (1) *Capacity accounting:* one-table outputs are bit-identical before and after the emulator change (checked on three saved outputs and 216 random comparisons by an independent QA probe); peak resident flows never exceed N (QA probe; unit test); D4s reported beside D4. This replaces the drafted "D4-degenerate equals the N/2 shipped run" gate, which needs a table-B-disabled switch that was not built; the substitute checks are stated here. (2) *Second-hash correlation:* on the active-tuple sets of both traces, the joint collision rate of A and B against the product of the marginals must lie in [0.8, 1.25]; a tabulation-hash B is the ideal reference and replaces the polynomial if the check fails. (3) *Under-loaded table:* the load sweep above, with benign gain reported apart from mitigation and the collision-refusal decomposition (refused by holder, refused by benign incumbent, displaced after holding, refused at a never-used slot) for every arm.
  - **Exploratory only.** Targeted arms (known-hash attackers need two holders per victim under D4; a keyed second hash removes targeting only until the mapping is probed, and a linear hash may leak it), D4 combined with D1 or D3, three or four choices, the model gate, f = 75 and 90%, D5 accuracy on PeerRush.
  - **Not claimed.** Deployability on Tofino-1: a paper analysis (`docs/lit/overnight_p4_feasibility.md`) finds that a full two-way split does not fit NetBeacon's 12 ingress stages and that a reduced variant (split identity registers only, packed tag and last-seen pair) might, unverified; the compile-only check is owed (`docs/sde_validation_request.md`). No claim that independent or keyed hashes add robustness; no protection against a hash-knowing, adaptive or budget-scaled attacker beyond the matched-refusal multiple; no claim that D4 works at any load; no claim that D4 eliminates the attack; no accuracy claim beyond this capture. Novelty: multi-choice hashing is established in flow tables (HashFlow, HashPipe and others); the claim is limited to its measured effect on classifier accuracy under slot-holding (`docs/lit/overnight_literature.md`; the literature check was shallow and partly used search snippets).
- 2026-09-24 (G6 addendum after an adversarial review of the G6 headline results; **everything below was decided after the D4 MAWI results, the D4 PeerRush f = 25% result and the load sweep were seen**, so these arms are exploratory or corrective, not confirmatory):
  - **Falsified prediction, stated as such.** The G6 prediction "D4s within ±0.1 of D4 in recovery" is **falsified**: measured R = 0.50 (D4s) against 0.76 (D4), a gap of 0.26. A post-hoc explanation, not tested: two ways of one set fail when a set holds two holders, about twice the rate of two independent slots (2ρ² against ρ²), which would roughly double the residual excess (about 0.52 predicted from that arithmetic). The load sweep also fell short of the pre-registered expectation that the benefit would persist until 2·q_b + q_h nears 1: recovery is 0.57 at about 12% benign occupancy, 0.07 at about 25% and −0.68 at about 50% (occupancy from the G5 amendment's median of 3,652 to 4,383 alive benign long flows on MAWI; the sweep holds nominal load per slot at 10%, so the attacker's absolute budget shrinks with the table).
  - **Verdict wording.** On PeerRush the pre-registered demotion rule was applied as written (strong by the draw interval, dropped to weak because the benign-flow interval for mitigation includes zero). A stricter reading ("weak requires the benign-flow interval to exclude zero") would make it inconclusive; both readings are reported. Recovery ratios with a small denominator (PeerRush E_und = 0.0144) are reported beside the mitigation difference.
  - **Added, exploratory:** (1) a benign-flow-cluster bootstrap for the MAWI recovery (the draws vary only the clock and the holder slots on a fixed capture); (2) D4 under the shipped flow-size (model) gate, 10 draws, primary day, f = 10%, with undefended partners; (3) a fixed-holder-count load sweep (6,554 holders against tables of 32,768, 16,384 and 8,192 slots, i.e. nominal f = 20, 40, 80%, 10 draws); (4) the doubled-table and D4s baselines on both days at 30 draws (primary) and 10 draws (replication); (5) a placement-policy ablation, **D4a**: a newcomer takes table A when takeable and table B otherwise, with no timestamp comparison (30 draws, primary day, f = 0 and 10%), because comparing two last-seen registers in one pass may cost a stage; (6) the collision-refusal decomposition and the MAWI M1 row (downgraded-packet share change, limit 0.5 percentage points) that G6 required and the first reports omitted. Holm: the reported intervals use a two-sided 97.5% level, which is the Holm bound for two comparisons, not four; raw sign-flip p-values are Monte Carlo and unadjusted, and are labelled so.
  - **Still not done, stated:** targeted (hash-knowing) attackers, adaptive holders, the pre-registered check that the shipped flow-size gate admits the holder pattern, and any switch compilation. No claim is made about them.
  - **Addendum 2 (after the D4a result was seen: with a table-A-first placement D4 fails the MAWI M1 bound, +0.54 percentage points of downgraded packets against a 0.5 limit, and recovery falls to 0.65).** A cheaper placement that needs no comparison of two last-seen registers is tested as **D4c**: a newcomer takes a never-claimed candidate if one exists (a tag-zero check), otherwise table A if takeable, otherwise table B. It runs at f = 0 and 10% on the primary day (30 draws) and the replication day (10 draws), with the same M1 rule and the same recovery definition. It is exploratory; a pass would not change the pre-registered D4 verdict, and the hardware cost of any placement policy is unverified.
  - **Addendum 3 (exploratory; decided after seeing that D4's recovery reverses when the attacker holds a large share of the table; the D3 rent results on PeerRush and MAWI were also known).** Question: does rent-style eviction help once D4 has removed most collisions? Arms, all on the fixed clock of D1 (no wrap window, takeover refresh, packet-gap fix) so that rent is defined: **D4d1** (D4 with the D1 clock, no rent; separates the clock effect), **D4age** (D4d1 plus age-only eviction of undetermined incumbents older than 1 s, when both candidates are held), and **D4cr8** is not run. Placement is the D4c rule (never-claimed candidate first, otherwise table A). Rent applies only when neither candidate is takeable; a takeable (idle, never-claimed or determined) candidate is always preferred. Runs: MAWI primary day, oracle gate, f = 0 and 10% at 65,536 slots (10 draws, clocks 0 to 9), and the fixed-budget sweep (6,554 holders against 32,768, 16,384 and 8,192 slots, 10 draws). Rules: the same recovery definition and M1 rule as G6; an arm is reported as helpful only if it (a) passes M1 on the no-attack runs and (b) raises recovery in the fixed-budget rows at 16,384 and 8,192 slots above D4c's (draw intervals not overlapping). Predictions: D4d1 is worse than D4c under attack (the wrap window's accidental eviction is gone, as in G5); D4age fails M1 or gains nothing at 65,536 slots and gains at 8,192 slots. No threshold or arm is changed after a look. Not claimed: any hardware cost for rent state on a two-table design.
  - **Outcome of addendum 3 and the late G6 items (recorded after the runs; `docs/results_g6_mawi.md`, `docs/results_g6_pr.md`).** Both addendum-3 predictions failed: D4d1 (D4 with the D1 clock) was better than D4c under attack on MAWI (recovery 0.87 against 0.77), and D4age gained at 65,536 slots (0.91) and 32,768 slots (0.63 against 0.44) but not at 16,384 and 8,192 slots (−0.34 against −0.33; −0.89 against −0.78). By the addendum's rule (M1 pass, and recovery above D4c's at both 16,384 and 8,192 slots with non-overlapping intervals) D4age is **not shown to be helpful**. D4d1 and D4age were not run on PeerRush. The D4c comparator was run at the fixed-budget sizes before this comparison, as the addendum requires. PeerRush D4s and D5 controls finished at 10 draws each: D5 matches D4; D4s is not clearly distinguishable from D4 on accuracy (R = 0.74 against 0.85, intervals overlap). The holder-pattern gate check (`docs/results_g6_gate_check.md`) was run after the results and passes for one header pattern.
- 2026-09-24 (G6 addendum 4, before any run of these arms; the targeted pilot and the G6 headline results are known; predictions come from `docs/lit/adaptive_arms_design.md`, derived by hand): **leaked-key-bits and rekeying arms** for the hash-aware attacker question. Emulator-level abstraction as before: the attacker is *given* knowledge, no probing procedure is modelled.
  - **Leak arm** (`scripts/g6_leak.py`). MAWI primary day, oracle gate, D4 (idle placement, shipped clock), 20 `vul` victims, holders start 0.5 s before each victim as in the targeted pilot. One holder holds the victim's table-A slot; the attacker knows the victim's table-B slot lies in a candidate set of size c (the true slot plus c − 1 random others) and places m holders on m distinct candidates. Cells (c, m): (1,1), (2,2), (2,1), (8,8), (8,4), (64,64), (64,32), plus the control (no informed table-B holder). 10 draws per cell (clocks 0 to 9). **Predictions:** victim downgrade share ≈ S0 + (1 − S0)·m/c, with S0 the control value (12 to 16% expected, from benign occupancy of table B); the m = c cells reach at least 90%. **Falsified if** an m = c cell has a lower 95% bound below 90% (one informed holder would then not be enough of a bound), or if any cell departs from the closed form by more than 0.15 absolute. Reported: holders per victim, attacker packets, slot-seconds, victim downgrade share and its excess over the control.
  - **Rekey arm** (`d5r<T>` in `scripts/g2_mawi.py`). D5 with the second hash redrawn every T seconds of the trace (epoch seed 900000 + 1000·epoch + clock start); a flow resident in table B loses its state at each rotation (its slot moves), table A is untouched. No-attack runs on the primary day, 10 draws, T = 5, 10, 30 and 60 s (the slice is 120 s, so longer periods are not measurable here and are not extrapolated). Metric: change in the downgraded-packet share against D4 with no rotation (percentage points), against the M1 limit of 0.5. **Prediction:** the cost falls with T and exceeds 0.5 pp at T = 5 s and 10 s. Reported only as a benign cost; whether any T also defeats an attacker who learns the key in seconds is discussed from the analytic probe estimate (about 11 to 35 probes for a small keyed space, one bit per probe as an assumption), not simulated.
  - **Not run:** moving-victim and adaptive-holder arms (the design gives closed forms; the age rule they test did not pass its own rule). Not claimed: crafting feasibility, hardware cost of rotation, protection beyond a constant factor against a hash-knowing attacker.
